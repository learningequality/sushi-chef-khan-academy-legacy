machine:
  python:
    version: 3.5.0
  environment:
    project: ka-lite
    version: 0.16
    sshuser: circleci

dependencies:
  override:
    - pip install pex

test:
  override:
    - py.test

deployment:
  pantry:
    branch: master
    commands:
      - make supported
      - make publish
